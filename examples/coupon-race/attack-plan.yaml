schema_version: "0.1"
id: double-hand-coupon
workflow_name: coupon-race-demo
attack_type: concurrent-replay
target_steps: [redeem-coupon]
session_bindings:
  redeem-coupon: alice
schedule:
  schema_version: "0.1"
  concurrency: 2
  offsets_ms: [0, 0]
  options:
    note: 仅描述计划；骨架不内置并发执行器
invariant_ids: [coupon-max-delta]
metadata:
  lab: 老王奶茶铺
