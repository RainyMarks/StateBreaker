name: statebreaker-ci

on:
  push:
  pull_request:

jobs:
  core:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.11", "3.12"]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - run: python -m pip install -e ".[dev]"
      - run: python -m ruff check .
      - run: python -m pytest
      - run: python -m mypy
      - run: python -m pip install -e ./plugin-template
      - run: python -m pip install -e ./race-generator
      - run: python -m pip install -e ./race-executor
      - run: python -m pip install -e ./statebreaker-learner-delta
      - run: python -m pytest race-generator/tests
      - run: python -m pytest race-executor/tests
      - run: python -m pytest statebreaker-learner-delta/tests
      - run: statebreaker plugins list

  docker-lab:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Build and start lab
        run: docker compose -f labs/coupon-race/compose.yaml up --build --wait --detach
      - name: Smoke test
        run: curl --fail --silent http://127.0.0.1:8080/healthz
      - name: Stop lab
        if: always()
        run: docker compose -f labs/coupon-race/compose.yaml down --volumes
